README

Compile the java files within the folder using the following command:

"javac *.java"

This will create two .class files within the same folder

To execute the program use the following command to execute the Main.class file:

"java Main"

This will start the program in the terminal / command prompt where you should be able to interact with the program.